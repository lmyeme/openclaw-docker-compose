
  
# 允许访问 Control UI 的浏览器源列表（完整 origin URL）  
# 影响：限制哪些域名/端口可以访问 Gateway 的 WebSocket 连接  
# 非-loopback 绑定必须配置此项，否则 Gateway 启动失败  
# 格式：["https://domain.com", "http://localhost:port"] (必须包含协议和端口)  
# 安全建议：生产环境使用 HTTPS，仅包含信任的域名  
ALLOWED_ORIGINS=["http://localhost:18789","https://domain.com:18789"]

# 网关配置
#OPENCLAW_GATEWAY_PORT=18789
#OPENCLAW_BRIDGE_PORT=18790
#OPENCLAW_GATEWAY_BIND=lan
OPENCLAW_GATEWAY_TOKEN= da19d0c37e71006d93bc376918940dde6706687c446766cf479fdf9a4cc37cf5

# 基础路径
OPENCLAW_CONFIG_DIR=./.openclaw
OPENCLAW_WORKSPACE_DIR=./.openclaw/workspace

# 工作空间配置（不要更改）
#WORKSPACE=/home/node/.openclaw/workspace

# 挂载目录配置（按实际更改）
# OpenClaw 数据目录（包含配置文件、工作空间等所有数据）
#OPENCLAW_DATA_DIR=~/.openclaw


# OPENCLAW_STATE_DIR=~/.openclaw
# OPENCLAW_CONFIG_PATH=~/.openclaw/openclaw.json


# Claude 集成（可选）
#CLAUDE_AI_SESSION_KEY=
#CLAUDE_WEB_SESSION_KEY=
#CLAUDE_WEB_COOKIE=
